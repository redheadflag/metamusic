services:
  backend:
    build: ./backend
    restart: unless-stopped
    env_file: .env
    volumes:
      - ${MUSIC_LIBRARY_PATH}:/music
    ports:
      - "${BACKEND_PORT:-8000}:8000"
    develop:
      watch:
        - action: sync+restart
          path: ./backend
          target: /app